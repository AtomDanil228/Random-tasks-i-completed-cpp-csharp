#include <iostream>
using namespace std;
int main()
{
    setlocale(LC_ALL, "rus");

    int a = 7, b = 8;

    int* ptr_a = &a;
    int* ptr_b = &b;
    if (*ptr_a > *ptr_b)
        cout << "Число a наибольшее.\n";
    else
        cout << "Число b наибольшее.\n";
}
